{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build app",
            "type": "shell",
            "command": "node",
            "args": ["index.js"],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
